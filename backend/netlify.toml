[build]
  command = "npm run build"
  publish = "build"       # ou dist si Vite
  functions = "functions"  # Dossier qui contient votre code Node.js

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200